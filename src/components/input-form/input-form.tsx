import s from './input.module.scss';

export const InputForm = () => {
  const title = '';

  return (
    <div className={s.input}>
      <input className={s.input__item} type='text' />
    </div>
  );
};
